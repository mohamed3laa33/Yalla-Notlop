<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :order_type %>
  </div>

  <div class="field">
    <%= form.label :orderFrom %>
    <%= form.text_field :orderFrom, id: :order_orderFrom %>
  </div>

  <div class="field">
    <%= form.label :menuImage %>
    <%= form.text_field :menuImage, id: :order_menuImage %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :order_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
